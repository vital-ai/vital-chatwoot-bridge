# Production Docker Compose configuration for ECS Fargate deployment
version: '3.8'

services:
  vital-chatwoot-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    image: vital-chatwoot-bridge:latest
    container_name: vital-chatwoot-bridge-prod
    ports:
      - "80:8000"
    environment:
      - CW_BRIDGE__app__port=8000
      - CW_BRIDGE__app__host=0.0.0.0
      - CW_BRIDGE__app__debug=false
      - CW_BRIDGE__app__environment=production
      - CW_BRIDGE__app__log_format=json
      # MemoryDB — set CW_BRIDGE__memorydb__url in .env or Secrets Manager
      - CW_BRIDGE__memorydb__ssl=true
      - CW_BRIDGE__memorydb__ssl_cert_reqs=none
      # Debounce (requires MemoryDB)
      - CW_BRIDGE__debounce__enabled=true
      - CW_BRIDGE__debounce__window_seconds=3.0
      - CW_BRIDGE__debounce__max_window_seconds=10.0
      - CW_BRIDGE__debounce__max_batch_size=10
      - CW_BRIDGE__debounce__dedup_ttl_seconds=60
      - CW_BRIDGE__debounce__drain_poll_interval=1.0
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - vital-bridge-network
    logging:
      driver: awslogs
      options:
        awslogs-group: "/ecs/vital-chatwoot-bridge"
        awslogs-region: "us-east-1"
        awslogs-stream-prefix: "ecs"
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

networks:
  vital-bridge-network:
    driver: bridge
